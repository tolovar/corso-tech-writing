Note veloci — domande che mi faccio aprendo il progetto

- Come lo avvio? `python src/main.py` basta o serve qualcos'altro?
- Devo avere Python installato? Quale versione devo usare?
- Dove sono le dipendenze? (non vedo `requirements.txt` o `pyproject.toml`)

- Perché `config_prod.json` è vuoto? Cosa ci devo mettere dentro?
- Cosa è `API_KEY_SECRET_V2`? Come la genero/ottengo e dove la metto?

- Che fa `process_data(x, y)`? Che tipi di input si aspettano x e y?
- Perché c'è `return y * 42`? Che significa 42 qui?
- Quando viene sollevato "Errore 99"? come lo risolvo nella pratica?

- `utils.js` è vuoto — serve davvero? posso eliminarlo?

- Dove finiscono i log? Come abilito una modalità debug per vedere cosa succede?

Piccolo piano per partire:
  1) Mettere un `README.md` con il comando minimo per avviare.
  2) Fornire un `config.example.json` o `.env.example` che mostri `API_KEY_SECRET_V2`.
  3) Documentare `process_data` con 1-2 esempi concreti.
